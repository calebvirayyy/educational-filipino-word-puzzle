import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Tagalog Word Puzzle Game',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
